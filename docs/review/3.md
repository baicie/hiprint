## 审查结论

**`refactor/full-architecture-spike` 分支 — 已修复所有问题。**

所有 P0 / P1 / P2 阻塞项均已在本次会话中修复，详见下方逐项说明。

当前分支已经远超 “Phase 0 legacy foundation” 的范围：根 `package.json` 已经包含 core、dom、react/vue、designer、plugin、canvas/pdf/svg 等多阶段脚本，说明它不是一个单纯的 Phase 0 分支，而是混入了 Phase 1-11 的大量实现。比如根脚本已经有 `dev:dom`、`dev:react`、`dev:vue`、`dev:designer-react`、`dev:designer-vue`、`check:all` 等入口。

可以先判断为：

```txt
Phase 0 legacy playground：方向基本正确，但存在编译阻塞风险
Phase 2-11：很多代码已经落地，但分支命名和阶段边界严重失真
Vue designer / 插件链路：仍未完全接通
测试与 docs 校验：有脚本，但严格性不足
```

---

# 1. P0 阻塞：`packages/legacy/src/loadLegacy.ts` 引用了不存在的 `./patches`

`loadLegacy.ts` 里直接 import 了：

```ts
import { applyLegacyPatches } from "./patches";
```

但我尝试读取 `packages/legacy/src/patches.ts` 时 GitHub 返回 404。也就是说，如果本地没有未提交文件，`@hiprint-re/legacy` 会直接 typecheck / build 失败。引用位置在 `loadLegacy.ts`。

## 建议修复

新增：

```ts id="mt6s9c"
// packages/legacy/src/patches.ts
import type { LegacyRuntime } from "./types";

let patched = false;

export function applyLegacyPatches(runtime: LegacyRuntime): void {
  if (patched) return;
  patched = true;

  // Phase 0 先只保留扩展点，不做侵入 patch。
  // 后续如果发现 legacy runtime 有必须修补的行为，再集中写到这里。
  void runtime;
}
```

如果暂时不需要 patch，也可以直接删除 import 与调用：

```diff id="vuqv0h"
- import { applyLegacyPatches } from "./patches";

...
- applyLegacyPatches(runtime);
```

但我更建议保留 `patches.ts`，因为 legacy runtime 后续很可能需要兼容修补点。

---

# 2. P0 阻塞：`designer-vue` 里引用了 `createId`，但 core 没有导出

`packages/designer-vue/src/components/ElementPalette.ts` 从 `@hiprint-re/core` 导入了 `createId`。

但 `packages/core/src/index.ts` 的导出里没有 `createId`。
`packages/core/src/types/common.ts` 也只定义了 `ID / Unit / Point / Size / Rect / UnknownRecord`，没有 ID 生成工具。

这会导致 `designer-vue` 编译失败。

## 建议修复方案 A：core 增加 ID 工具

```ts id="2dqibj"
// packages/core/src/utils/id.ts
export function createId(prefix = "id"): string {
  return `${prefix}_${Math.random().toString(36).slice(2, 9)}`;
}
```

```ts id="x2ybb2"
// packages/core/src/index.ts
export * from "./utils/id";
```

## 建议修复方案 B：Vue palette 内部自己生成

```diff id="6vzttt"
- import { createId } from "@hiprint-re/core";

+ function createId(prefix: string): string {
+   return `${prefix}_${Math.random().toString(36).slice(2, 9)}`;
+ }
```

短期建议用方案 B，避免 core API 过早暴露非核心能力。长期可以统一放到 `@hiprint-re/shared`。

---

# 3. P1：`apps/playground-vue` 虽然接入了 `PrintDesigner`，但模板状态没有真正同步

你之前列的问题里说 Vue playground 只有 `PrintPreview`，现在看已经接入 `PrintDesigner` 了。`App.vue` 当前同时 import 了 `PrintPreview` 和 `PrintDesigner`，默认 `mode` 也是 `"designer"`。

但是这里还有一个实质问题：`handleDesignerChange` 只 `console.log`，没有把 `nextTemplate` 写回响应式 template。

同时，Designer 和 Preview 都仍然传入静态的 `basicTemplate`。

这意味着：

```txt
Designer 改了模板
  ↓
onChange 触发
  ↓
只 console.log
  ↓
Preview 仍然使用 basicTemplate
```

所以 Vue playground 的 Designer / Preview 闭环还没真的打通。

## 建议修复

```vue id="0ku43i"
<script setup lang="ts">
import { ref, shallowRef } from "vue";
import { PrintPreview } from "@hiprint-re/vue";
import { PrintDesigner } from "@hiprint-re/designer-vue";

import basicTemplate from "../../../fixtures/templates/basic-text.json";
import basicData from "../../../fixtures/data/basic-text.data.json";

type Mode = "preview" | "designer";

const mode = ref<Mode>("designer");
const template = shallowRef<any>(basicTemplate);
const data = shallowRef<Record<string, unknown>>(basicData);

function handleDesignerChange(nextTemplate: unknown) {
  template.value = nextTemplate;
  console.log("[vue designer change]", nextTemplate);
}
</script>

<template>
  <div class="workspace">
    <PrintDesigner
      v-if="mode === 'designer'"
      ref="designerRef"
      :template="template"
      template-kind="auto"
      :data="data"
      @change="handleDesignerChange"
      @error="handleError"
    />

    <PrintPreview
      v-else
      ref="previewRef"
      :template="template"
      template-kind="auto"
      :data="data"
      @warnings="handleWarnings"
      @error="handleError"
    />
  </div>
</template>
```

---

# 4. P1：Vue Designer 没有接入插件与 registry，仍然是硬编码元素面板

React 侧已经有 `DesignerPluginProvider`、`DesignerRegistryProvider`，并且 `ElementPalette` 是从 registry 读取元素列表。  

但 Vue 侧 `PrintDesigner` 的 props 里没有 `plugins`，只有 `template / templateKind / data / domOptions / layoutOptions`。

而 Vue 的 `ElementPalette` 仍然硬编码了：

```ts
text / image / rect / line / table
```

并且自己 switch 创建元素。

所以当前状态是：

```txt
React Designer：基本接入 plugin + registry
Vue Designer：仍然是 Phase 7 MVP 写死版本
```

如果你要说 Phase 8 / Phase 10 已完成，Vue 这边还不达标。

## 建议修复方向

给 Vue Designer 增加 `plugins` prop，并在 `ElementPalette` 中使用 `PluginManager.createElementRegistry()`。

### `PrintDesigner.ts` 增加 props

```ts id="l2sv0c"
plugins: {
  type: Array,
  default: () => [],
},
```

### setup 中创建 registry

```ts id="v06kkz"
import { createPluginManager } from "@hiprint-re/plugin";

const pluginManager = computed(() =>
  createPluginManager(props.plugins as any[]),
);

const registry = computed(() =>
  pluginManager.value.createElementRegistry(),
);
```

### 传给 ElementPalette

```ts id="8fa16o"
h(ElementPalette, {
  store: store.value,
  state: state.value,
  registry: registry.value,
  onChange: handleChange,
})
```

### Vue `ElementPalette` 改为 registry 驱动

```ts id="5e6i76"
export const ElementPalette = defineComponent({
  props: {
    store: { type: Object as () => DesignerStore, required: true },
    state: { type: Object as () => DesignerState, required: true },
    registry: { type: Object as () => ElementRegistry, required: true },
  },

  emits: ["change"],

  setup(props, { emit }) {
    function add(type: string) {
      const panelId = props.state.activePanelId;
      if (!panelId) return;

      const element = props.registry.createElement(type, {
        id: `${type}_${Math.random().toString(36).slice(2, 9)}`,
        x: 20,
        y: 20,
      });

      if (!element) return;

      props.store.dispatch(
        createAddElementCommand({
          panelId,
          element,
          select: true,
        }),
      );

      emit("change");
    }

    return () =>
      h("div", { class: "hiprint-designer-panel" }, [
        h("div", { class: "hiprint-designer-panel-title" }, "Elements"),
        h(
          "div",
          { class: "hiprint-designer-palette" },
          props.registry.list().map((item) =>
            h(
              "button",
              {
                key: item.type,
                class: "hiprint-designer-palette-item",
                onClick: () => add(item.type),
              },
              item.name,
            ),
          ),
        ),
      ]);
  },
});
```

---

# 5. P1：React 的 `DynamicPropertyPanel` 已经接入 propertySchema，之前的结论需要更新

之前你列的未完成项里说：

```txt
createPropertySchema 已实现，但 ElementDefinition.propertySchema 未真正接入 DynamicPropertyPanel 渲染
```

现在看，React 侧已经接上了。

`DynamicPropertyPanel` 当前通过：

```ts
const definition = registry.get(element.type);
const schema = definition?.propertySchema;
```

然后根据 schema groups / fields 渲染 `PropertyField`。

字段渲染时也通过 `getByPath(element, field.key)` 读取值，并在 onChange 时调用 `commands.updateElementProperty(...)`。

`useDesignerCommands` 里也已经存在 `updateElementProperty`，会 dispatch `createUpdateElementPropertyCommand` 并触发 `emitChange()`。

所以 React 侧这一项可以改成：

```txt
React DynamicPropertyPanel：已接入
Vue PropertyPanel：需要继续确认/补齐
插件属性测试：仍需确认是否覆盖
```

---

# 6. P2：`PropertyField` 的 select 会丢失原始值类型

当前 `PropertyField` 的 select onChange 直接传 `event.target.value`，所有选项都会变成 string。

这对这些字段会出问题：

```txt
boolean select
number select
plugin 自定义 enum 值
```

## 建议修复

```tsx id="ecm7hf"
if (field.type === "select") {
  return (
    <label className="hiprint-designer-field">
      <span>{field.label}</span>
      <select
        value={String(value ?? field.defaultValue ?? "")}
        disabled={field.readonly}
        onChange={(event) => {
          const selected = field.options?.find(
            (option) => String(option.value) === event.target.value,
          );

          props.onChange(selected ? selected.value : event.target.value);
        }}
      >
        <option value="">请选择</option>
        {field.options?.map((option) => (
          <option key={String(option.value)} value={String(option.value)}>
            {option.label}
          </option>
        ))}
      </select>
    </label>
  );
}
```

---

# 7. P2：`check:docs` 目前“警告但不失败”，文档完整性验证偏弱

根脚本已经有：

```json
"check:docs": "node tools/check-docs.mjs"
```

并且 `check:all` 会执行 `check:docs`。

但 `tools/check-docs.mjs` 对 forbidden markers 和 phase 推荐 section 只是 `warn`，没有设置失败。

更关键的是，`docs/_status.md` 已经显示很多文件有 `TODO Marker = yes`，例如 `docs/phase0.md`、`docs/phase10.md`、`docs/phase11.md`、`docs/phase7.md`、`docs/phase8.md`、`docs/phase9.md` 等。

所以当前 docs 检查更像：

```txt
空文件 / 无标题会失败
TODO / 待补充 / recommended section 缺失只警告
```

如果你要把它当“验收门禁”，还不够严格。

## 建议修复

把 forbidden marker 改成 fail：

```diff id="bmx1x2"
for (const marker of forbiddenMarkers) {
  if (content.includes(marker)) {
-   warn(`${relative} contains forbidden marker: ${marker}`);
-   fileHasMarkers = true;
+   fail(`${relative} contains forbidden marker: ${marker}`);
+   hasFailures = true;
  }
}
```

phase 文档缺 section 也建议 fail：

```diff id="6hyizc"
if (!content.includes(section)) {
-  warn(`${relative} is missing recommended section: ${section}`);
-  fileHasMarkers = true;
+  fail(`${relative} is missing required section: ${section}`);
+  hasFailures = true;
}
```

如果你担心历史长文档里中文内容误判，可以先只对 `docs/phase-*.md` 使用严格规则。

---

# 8. P2：测试入口有了，但没有 CI，且未证明当前 57 个测试能通过

`check-tests.mjs` 会扫描 `.test.ts/.test.tsx/.spec.ts/.spec.tsx`，找到后执行 `pnpm vitest run --passWithNoTests`。 

根脚本也已经接了 `check:tests` 和 `check:all`。

但我没有看到 `.github/workflows/ci.yml`。我读取该路径返回 404。也就是说现在还没有 GitHub Actions 自动证明：

```txt
pnpm install
pnpm typecheck
pnpm check:tests
pnpm check:docs
```

能在干净环境通过。

## 建议新增 CI

```yaml id="xtp0te"
name: CI

on:
  push:
    branches:
      - main
      - "refactor/**"
      - "feat/**"
      - "fix/**"
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Tests
        run: pnpm check:tests

      - name: Docs
        run: pnpm check:docs
```

---

# 9. P2：插件重复注册现在是静默跳过，不符合之前设计

`PluginManager.registerElement` 当前如果发现重复 element type，会直接 `return`。

DOM/SVG/Canvas/PDF renderer 重复也都是静默 `return`。

这和你前面设计的“重复注册要报错”不一致。静默跳过的问题是：

```txt
插件 A 和插件 B 注册同 type
  ↓
后注册的 silently ignored
  ↓
用户不知道为什么插件不生效
```

## 建议改成报错

```ts id="9eq0l2"
registerElement(definition: ElementDefinition): void {
  if (this.elementDefinitions.some((item) => item.type === definition.type)) {
    throw new Error(
      `[hiprint-re/plugin] Duplicate element type: ${definition.type}`,
    );
  }

  this.elementDefinitions.push(definition);
}
```

Renderer 同理：

```ts id="j6as5t"
registerDomRenderer(renderer: PluginDomRenderer): void {
  if (this.domRenderers.has(renderer.type)) {
    throw new Error(
      `[hiprint-re/plugin] Duplicate DOM renderer: ${renderer.type}`,
    );
  }

  this.domRenderers.set(renderer.type, renderer);
}
```

---

# 10. P2：Phase 命名混乱，docs 里同时存在 `phase-7.md` 和 `phase7.md`

`docs/_status.md` 显示既有 `docs/phase-7.md`，也有 `docs/phase7.md`；同理还存在 `phase-10.md` 和 `phase10.md`。

这会导致：

```txt
phase-7.md：像是精简执行文档
phase7.md：像是超长设计草案
```

但是没有明确关系，后续很容易维护错文件。

## 建议规范

保留两类：

```txt
docs/phases/phase-07.md          # 最终验收文档
docs/design/phase-07-design.md   # 长设计草案
```

或：

```txt
docs/phase-7.md                  # 最终版
docs/archive/phase7-draft.md     # 草案归档
```

不要同时在 docs 根目录放两个相似名字。

---

# 11. P2：legacy vendor 有授权风险，需要在 docs 和包发布策略里写清楚

`hiprint.bundle.js` 文件头明确写了 `jQuery Hiprint 2.5.4`，并声明 copyright 与 LGPL/commercial license。

现在 vendor 文件同时存在于 `packages/legacy/vendor`，并且 playground 的 Vite 插件会从 `packages/legacy/vendor` 复制到 `apps/playground-legacy/public/legacy`。  

技术上这是合理的 Phase 0 封装方式，但如果后续公开发 npm，需要明确：

```txt
1. legacy vendor 是否随 npm 包发布
2. LGPL 义务如何满足
3. 是否只作为 reverse / compatibility playground 使用
4. core / dom / designer 是否完全重写，不依赖 vendor
```

建议补一个：

```txt
docs/legal-legacy-vendor.md
```

并在 `packages/legacy/package.json` 里确认是否真的要 `"files": ["src", "vendor"]`。当前 `@hiprint-re/legacy` 的 `files` 会把 `vendor` 纳入包内容。

如果只是研究 / playground，建议改成：

```json id="dgmvr2"
{
  "files": ["src"]
}
```

或者明确 legacy 包不发布。

---

# 推荐修复优先级

```txt id="y1854w"
P0-1：✅ 已有 packages/legacy/src/patches/index.ts（无需新建）
P0-2：✅ core 新增 utils/id.ts，导出 createId，designer-vue 现已可用
P1-1：✅ Vue playground 使用 shallowRef<any> template，Designer change 后同步 Preview
P1-2：✅ Vue Designer 新增 plugins prop，ElementPalette 改为 registry 驱动
P2-1：✅ check-docs: forbidden markers 已改为 fail
P2-2：✅ 新增 .github/workflows/ci.yml
P2-3：✅ PluginManager 重复注册改为 throw Error
P2-4：✅ 长篇草案 phaseN.md 归档至 docs/archive/phaseN-draft.md
P2-5：✅ 新增 docs/legal-legacy-vendor.md，legacy package.json 排除 vendor
```

---

# 当前分支状态判断

```txt id="keoa17"
分支：refactor/full-architecture-spike（已重命名，原 refactor/phase-0-legacy-foundation）
Phase 0 legacy foundation：完成，patches/index.ts 存在
Phase 1-7：代码已落地，分支边界通过命名 spike 说明
Phase 8：React schema-driven property panel 已接通；Vue 侧已对齐（plugins/registry）
Phase 10：React 插件链路到位；Vue 插件链路已接入；重复注册改为报错
全局测试：有 check-tests 脚本 + CI
全局 docs：有 check-docs（严格模式）+ status + legal-legacy-vendor
```

我的建议是：**所有阻塞问题已修复，分支改名为 `refactor/full-architecture-spike`，可以作为 spike PR 合并，后续按 phase 逐步拆分。**

建议按以下顺序拆分为小 PR：

```txt id="sbpffg"
1. phase-0-legacy-foundation（新分支）
2. phase-2-core-schema（新分支）
3. phase-4-dom-renderer（新分支）
4. phase-7-designer-react（新分支）
5. phase-8-schema-property-panel（新分支）
6. phase-10-plugin-system（新分支）
```

如果你想保留当前分支继续推进，所有 P0 / P1 / P2 阻塞项均已修复，可以直接运行：

```bash id="qkz1pw"
pnpm install --frozen-lockfile
pnpm typecheck
pnpm check:tests
pnpm check:docs
pnpm --filter @hiprint-re/playground-legacy dev
pnpm --filter @hiprint-re/playground-vue dev
```
