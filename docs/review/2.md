基于你列的未达标项，补齐重点不是新增大功能，而是把 **Phase 8 / Phase 10 的“接线”和“验收闭环”补完整**。你前面的路线文档里也明确了 Vue designer 应与 React designer 共享 core / fixtures，并完成功能对齐，这几个 P2 问题正好属于这个层面的收口工作。

下面按 5 个 PR / 任务给你详细设计与代码草案。

---

# 总体代做清单

```txt
PR 1：fix(playground-vue): integrate PrintDesigner into vue playground
PR 2：fix(designer): wire ElementDefinition.propertySchema to DynamicPropertyPanel
PR 3：test(plugin): verify plugin propertySchema rendering and update flow
PR 4：test(repo): add global test/typecheck/docs verification scripts
PR 5：docs(repo): add docs completeness checker and docs status report
```

最终验收目标：

```txt
1. apps/playground-vue 同时支持 PrintPreview / PrintDesigner
2. ElementDefinition.propertySchema 真正驱动 DynamicPropertyPanel
3. 插件元素 propertySchema 能显示、能编辑、能写回 template
4. 57 个测试文件可通过统一命令验证
5. 32 个 docs 文件有完整性检查脚本
```

---

# PR 1：Phase 8 Vue Designer Playground 补齐

## 目标

当前问题：

```txt
apps/playground-vue/ 只有 PrintPreview
未接入 PrintDesigner
```

应该改成：

```txt
apps/playground-vue
  ├─ Preview 模式
  └─ Designer 模式
```

这样既保留 Phase 5 的 Vue Preview 验证，也补上 Phase 8 的 Vue Designer 验证。

---

## 1.1 修改 `apps/playground-vue/package.json`

```json
{
  "name": "@hiprint-re/playground-vue",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hiprint-re/core": "workspace:*",
    "@hiprint-re/vue": "workspace:*",
    "@hiprint-re/designer-vue": "workspace:*",
    "@hiprint-re/plugins-basic": "workspace:*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.0",
    "vue-tsc": "^3.0.0"
  }
}
```

如果现在 `@hiprint-re/designer-vue` 还没完全成熟，也可以先接入 MVP 版本，只要 `PrintDesigner` 能渲染、能新增元素、能触发 `change` 即可。

---

## 1.2 修改 `apps/playground-vue/src/App.vue`

```vue
<script setup lang="ts">
import { ref, shallowRef } from "vue"
import { PrintPreview } from "@hiprint-re/vue"
import { PrintDesigner } from "@hiprint-re/designer-vue"
import {
  barcodePlugin,
  qrcodePlugin,
  htmlPlugin,
} from "@hiprint-re/plugins-basic"

import basicTemplate from "../../../fixtures/templates/basic-text.json"
import basicData from "../../../fixtures/data/basic-text.data.json"

type Mode = "preview" | "designer"

const mode = ref<Mode>("designer")
const template = shallowRef<any>(basicTemplate)
const data = shallowRef<any>(basicData)

const plugins = [
  barcodePlugin(),
  qrcodePlugin(),
  htmlPlugin(),
]

const designerRef = ref<{
  refresh?: () => void
  print?: () => Promise<void>
  getTemplate?: () => unknown
} | null>(null)

const previewRef = ref<{
  refresh?: () => void
  print?: () => Promise<void>
  getLayout?: () => unknown
} | null>(null)

function setMode(nextMode: Mode) {
  mode.value = nextMode
}

function handleDesignerChange(nextTemplate: unknown) {
  template.value = nextTemplate
  console.log("[vue designer change]", nextTemplate)
}

function handleWarnings(warnings: unknown[]) {
  console.warn("[vue playground warnings]", warnings)
}

function handleError(error: Error) {
  console.error("[vue playground error]", error)
}

async function print() {
  if (mode.value === "designer") {
    await designerRef.value?.print?.()
    return
  }

  await previewRef.value?.print?.()
}
</script>

<template>
  <div class="app">
    <div class="toolbar">
      <button
        :class="{ active: mode === 'designer' }"
        @click="setMode('designer')"
      >
        Designer
      </button>

      <button
        :class="{ active: mode === 'preview' }"
        @click="setMode('preview')"
      >
        Preview
      </button>

      <span class="separator" />

      <button @click="print">
        Print
      </button>
    </div>

    <div class="workspace">
      <PrintDesigner
        v-if="mode === 'designer'"
        ref="designerRef"
        :template="template"
        template-kind="auto"
        :data="data"
        :plugins="plugins"
        @change="handleDesignerChange"
        @error="handleError"
      />

      <PrintPreview
        v-else
        ref="previewRef"
        :template="template"
        template-kind="auto"
        :data="data"
        @warnings="handleWarnings"
        @error="handleError"
      />
    </div>
  </div>
</template>
```

---

## 1.3 修改 `apps/playground-vue/src/style.css`

```css
html,
body,
#app,
.app {
  width: 100%;
  height: 100%;
  margin: 0;
}

* {
  box-sizing: border-box;
}

body {
  font-family:
    Inter,
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;
}

.app {
  display: grid;
  grid-template-rows: 48px 1fr;
  background: #f3f4f6;
}

.toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 0 16px;
  border-bottom: 1px solid #e5e7eb;
  background: #fff;
}

.toolbar button {
  height: 32px;
  padding: 0 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  background: #fff;
  cursor: pointer;
}

.toolbar button.active {
  border-color: #2563eb;
  color: #2563eb;
  background: #eff6ff;
}

.separator {
  width: 1px;
  height: 24px;
  margin: 0 4px;
  background: #e5e7eb;
}

.workspace {
  min-height: 0;
  overflow: hidden;
}
```

---

## 1.4 验收标准

```txt
pnpm --filter @hiprint-re/playground-vue dev

页面应满足：
1. 默认进入 Designer 模式
2. 可以切换 Preview 模式
3. Designer 内能显示元素面板 / 画布 / 属性面板
4. 修改模板后 Preview 使用更新后的 template
5. 插件元素 barcode/qrcode/html 能出现在 Vue Designer 元素面板中
```

---

# PR 2：ElementDefinition.propertySchema 接入 DynamicPropertyPanel

## 目标

当前问题：

```txt
createPropertySchema 已实现
ElementDefinition.propertySchema 未真正驱动 DynamicPropertyPanel
```

正确链路应该是：

```txt
ElementDefinition.propertySchema
  ↓
ElementRegistry.get(element.type)
  ↓
DynamicPropertyPanel
  ↓
PropertyField
  ↓
createUpdateElementPropertyCommand
  ↓
template 更新
```

关键点：**属性路径更新应该放到 designer-core，不要放在 React/Vue UI 里硬改对象。**

---

## 2.1 在 `designer-core` 新增 path 工具

### `packages/designer-core/src/utils/path.ts`

```ts
export function getByPath(
  target: unknown,
  path: string,
): unknown {
  const parts = path.split(".")
  let current: any = target

  for (const part of parts) {
    if (current == null) return undefined
    current = current[part]
  }

  return current
}

export function setByPathImmutable<T>(
  target: T,
  path: string,
  value: unknown,
): T {
  const clone = cloneValue(target)
  const parts = path.split(".")

  let current: any = clone

  for (let index = 0; index < parts.length - 1; index++) {
    const part = parts[index]!

    if (
      current[part] == null ||
      typeof current[part] !== "object" ||
      Array.isArray(current[part])
    ) {
      current[part] = {}
    }

    current = current[part]
  }

  current[parts[parts.length - 1]!] = value

  return clone
}

function cloneValue<T>(value: T): T {
  if (typeof structuredClone === "function") {
    return structuredClone(value)
  }

  return JSON.parse(JSON.stringify(value)) as T
}
```

---

## 2.2 新增 `createUpdateElementPropertyCommand`

### `packages/designer-core/src/command/commands/updateElementProperty.ts`

```ts
import type { DesignerCommand } from "../types"
import { updateElementById } from "./utils"
import { setByPathImmutable } from "../../utils/path"

export interface UpdateElementPropertyCommandInput {
  id: string
  path: string
  value: unknown
}

export function createUpdateElementPropertyCommand(
  input: UpdateElementPropertyCommandInput,
): DesignerCommand {
  return {
    id: "element.property.update",
    name: "Update Element Property",
    history: true,

    execute(state) {
      return {
        ...state,
        template: updateElementById(
          state.template,
          input.id,
          (element) =>
            setByPathImmutable(
              element,
              input.path,
              input.value,
            ),
        ),
      }
    },
  }
}
```

---

## 2.3 导出 command

### `packages/designer-core/src/command/index.ts`

```ts
export * from "./commands/updateElementProperty"
```

### `packages/designer-core/src/index.ts`

确保已有：

```ts
export * from "./command"
export * from "./utils/path"
```

---

## 2.4 修改 `useDesignerCommands`

### `packages/designer-react/src/hooks/useDesignerCommands.ts`

增加：

```ts
import {
  createUpdateElementPropertyCommand,
} from "@hiprint-re/designer-core"
```

在返回对象里新增：

```ts
updateElementProperty(
  id: string,
  path: string,
  value: unknown,
) {
  store.dispatch(
    createUpdateElementPropertyCommand({
      id,
      path,
      value,
    }),
  )

  emitChange()
},
```

最终相关片段：

```ts
export function useDesignerCommands() {
  const { store, onChange } = useDesignerContext()

  return useMemo(() => {
    function emitChange() {
      onChange?.(store.getStateRef().template)
    }

    return {
      updateElementProperty(
        id: string,
        path: string,
        value: unknown,
      ) {
        store.dispatch(
          createUpdateElementPropertyCommand({
            id,
            path,
            value,
          }),
        )

        emitChange()
      },

      // existing commands...
    }
  }, [store, onChange])
}
```

---

## 2.5 修改 `DynamicPropertyPanel`

### `packages/designer-react/src/components/DynamicPropertyPanel.tsx`

```tsx
import {
  getByPath,
  getElementById,
} from "@hiprint-re/designer-core"
import { useDesignerState } from "../hooks/useDesignerState"
import { useDesignerCommands } from "../hooks/useDesignerCommands"
import { useDesignerRegistry } from "../registry/DesignerRegistryContext"
import { PropertyField } from "./PropertyField"

export function DynamicPropertyPanel() {
  const state = useDesignerState()
  const commands = useDesignerCommands()
  const registry = useDesignerRegistry()

  const activeId = state.selection.activeId
  const element = activeId
    ? getElementById(state, activeId)
    : undefined

  if (!element) {
    return (
      <div className="hiprint-designer-panel">
        <div className="hiprint-designer-panel-title">
          Properties
        </div>
        <div className="hiprint-designer-empty">
          No element selected
        </div>
      </div>
    )
  }

  const definition = registry.get(element.type)
  const schema = definition?.propertySchema

  if (!schema) {
    return (
      <div className="hiprint-designer-panel">
        <div className="hiprint-designer-panel-title">
          Properties
        </div>
        <div className="hiprint-designer-empty">
          No property schema for {element.type}
        </div>
      </div>
    )
  }

  const sortedGroups = [...schema.groups].sort(
    (a, b) => (a.order ?? 0) - (b.order ?? 0),
  )

  return (
    <div className="hiprint-designer-panel">
      <div className="hiprint-designer-panel-title">
        Properties · {definition.name}
      </div>

      {sortedGroups.map((group) => {
        const fields = schema.fields.filter(
          (field) => (field.group ?? "base") === group.key,
        )

        if (fields.length === 0) return null

        return (
          <div
            key={group.key}
            className="hiprint-designer-property-group"
          >
            <div className="hiprint-designer-property-group-title">
              {group.label}
            </div>

            {fields.map((field) => (
              <PropertyField
                key={field.key}
                field={field}
                value={getByPath(element, field.key)}
                onChange={(value) => {
                  commands.updateElementProperty(
                    element.id,
                    field.key,
                    value,
                  )
                }}
              />
            ))}
          </div>
        )
      })}
    </div>
  )
}
```

---

## 2.6 修改 `PropertyField`

要保证 `number / boolean / json / select` 的值类型正确。

### `packages/designer-react/src/components/PropertyField.tsx`

```tsx
import type { PropertyFieldSchema } from "@hiprint-re/core"

export interface PropertyFieldProps {
  field: PropertyFieldSchema
  value: unknown
  onChange: (value: unknown) => void
}

export function PropertyField(props: PropertyFieldProps) {
  const { field } = props

  if (field.hidden) return null

  const value = props.value ?? field.defaultValue ?? ""

  if (field.type === "boolean") {
    return (
      <label className="hiprint-designer-field is-checkbox">
        <span>{field.label}</span>
        <input
          type="checkbox"
          checked={Boolean(value)}
          disabled={field.readonly}
          onChange={(event) => props.onChange(event.target.checked)}
        />
      </label>
    )
  }

  if (field.type === "select") {
    return (
      <label className="hiprint-designer-field">
        <span>{field.label}</span>
        <select
          value={String(value)}
          disabled={field.readonly}
          onChange={(event) => {
            const option = field.options?.find(
              (item) => String(item.value) === event.target.value,
            )

            props.onChange(option ? option.value : event.target.value)
          }}
        >
          <option value="">Please select</option>
          {field.options?.map((option) => (
            <option
              key={String(option.value)}
              value={String(option.value)}
            >
              {option.label}
            </option>
          ))}
        </select>
      </label>
    )
  }

  if (field.type === "textarea") {
    return (
      <label className="hiprint-designer-field">
        <span>{field.label}</span>
        <textarea
          value={String(value)}
          disabled={field.readonly}
          placeholder={field.placeholder}
          onChange={(event) => props.onChange(event.target.value)}
        />
      </label>
    )
  }

  if (field.type === "json") {
    return (
      <label className="hiprint-designer-field">
        <span>{field.label}</span>
        <textarea
          value={formatJsonValue(value)}
          disabled={field.readonly}
          onChange={(event) => {
            try {
              props.onChange(JSON.parse(event.target.value))
            } catch {
              props.onChange(event.target.value)
            }
          }}
        />
      </label>
    )
  }

  return (
    <label className="hiprint-designer-field">
      <span>{field.label}</span>
      <input
        type={toInputType(field.type)}
        value={String(value)}
        min={field.min}
        max={field.max}
        step={field.step}
        placeholder={field.placeholder}
        readOnly={field.readonly}
        onChange={(event) => {
          if (field.type === "number") {
            const next = Number(event.target.value)
            props.onChange(Number.isFinite(next) ? next : 0)
            return
          }

          props.onChange(event.target.value)
        }}
      />
    </label>
  )
}

function toInputType(type: string): string {
  if (type === "number") return "number"
  if (type === "color") return "color"
  return "text"
}

function formatJsonValue(value: unknown): string {
  if (typeof value === "string") return value

  try {
    return JSON.stringify(value ?? {}, null, 2)
  } catch {
    return ""
  }
}
```

---

## 2.7 验收标准

```txt
1. Text 元素选中后，DynamicPropertyPanel 显示 Geometry / Binding / Content / Style
2. 修改 style.fontSize 能写回 template
3. 修改 options.content 能写回 template
4. 修改 binding.field 能写回 template
5. 不再依赖 element.type === "text" 的大量分支
```

---

# PR 3：Phase 10 插件元素属性渲染验证

## 目标

当前问题：

```txt
DynamicPropertyPanel 是否真正能渲染插件元素属性未验证
```

补测试：

```txt
1. 注册自定义插件元素
2. palette 显示插件元素
3. 点击新增插件元素
4. 属性面板显示插件 propertySchema
5. 修改插件字段
6. onChange 返回的 template 中插件元素被正确更新
```

---

## 3.1 新增测试用插件

### `tests/helpers/testPlugin.ts`

```ts
import {
  createPlugin,
} from "../../packages/plugin/src"
import {
  createPropertySchema,
  type ElementDefinition,
} from "../../packages/core/src"

export const testCustomElementDefinition: ElementDefinition = {
  type: "test:custom-box",
  name: "Custom Box",
  defaultWidth: 50,
  defaultHeight: 20,

  propertySchema: createPropertySchema({
    groups: [
      {
        key: "geometry",
        label: "Geometry",
      },
      {
        key: "custom",
        label: "Custom",
      },
    ],
    fields: [
      {
        key: "x",
        label: "X",
        type: "number",
        group: "geometry",
      },
      {
        key: "y",
        label: "Y",
        type: "number",
        group: "geometry",
      },
      {
        key: "options.label",
        label: "Label",
        type: "text",
        group: "custom",
      },
      {
        key: "options.enabled",
        label: "Enabled",
        type: "boolean",
        group: "custom",
        defaultValue: true,
      },
    ],
  }),

  createElement(input) {
    return {
      id: input.id,
      type: "test:custom-box",
      x: input.x,
      y: input.y,
      width: 50,
      height: 20,
      options: {
        label: "Custom Label",
        enabled: true,
      },
      style: {
        borderColor: "#111827",
      },
    }
  },
}

export function testCustomPlugin() {
  return createPlugin({
    name: "test-custom-plugin",
    version: "0.0.0",
    elements: [testCustomElementDefinition],
    domRenderers: [
      {
        type: "test:custom-box",
        render(element, ctx) {
          const root = ctx.document.createElement("div")
          root.dataset.elementType = "test:custom-box"
          root.dataset.elementId = element.id
          root.textContent = "Custom Box"
          root.style.position = "absolute"
          root.style.left = `${element.x}${ctx.options.geometryUnit}`
          root.style.top = `${element.y}${ctx.options.geometryUnit}`
          root.style.width = `${element.width}${ctx.options.geometryUnit}`
          root.style.height = `${element.height}${ctx.options.geometryUnit}`
          return root
        },
      },
    ],
  })
}
```

---

## 3.2 新增测试：插件属性面板

### `tests/designer-react/plugin-property-schema.test.tsx`

```tsx
import {
  describe,
  expect,
  it,
  vi,
} from "vitest"
import {
  fireEvent,
  render,
  waitFor,
} from "@testing-library/react"
import {
  createEmptyTemplate,
} from "../../packages/core/src"
import {
  PrintDesigner,
} from "../../packages/designer-react/src"
import {
  testCustomPlugin,
} from "../helpers/testPlugin"

describe("plugin property schema", () => {
  it("should render and update plugin element properties", async () => {
    const onChange = vi.fn()

    const { getByText, getByLabelText } = render(
      <div style={{ width: 1000, height: 800 }}>
        <PrintDesigner
          template={createEmptyTemplate()}
          templateKind="core"
          plugins={[testCustomPlugin()]}
          onChange={onChange}
        />
      </div>,
    )

    fireEvent.click(getByText("Custom Box"))

    await waitFor(() => {
      expect(getByText("Custom")).toBeTruthy()
      expect(getByLabelText("Label")).toBeTruthy()
    })

    fireEvent.change(getByLabelText("Label"), {
      target: {
        value: "Updated Plugin Label",
      },
    })

    await waitFor(() => {
      expect(onChange).toHaveBeenCalled()
    })

    const latestTemplate =
      onChange.mock.calls[onChange.mock.calls.length - 1]?.[0]

    const element = latestTemplate.panels[0].elements.find(
      (item: any) => item.type === "test:custom-box",
    )

    expect(element.options.label).toBe("Updated Plugin Label")
  })
})
```

---

## 3.3 新增测试：插件 renderer 接入

### `tests/designer-react/plugin-renderer.test.tsx`

```tsx
import {
  describe,
  expect,
  it,
} from "vitest"
import {
  fireEvent,
  render,
  waitFor,
} from "@testing-library/react"
import {
  createEmptyTemplate,
} from "../../packages/core/src"
import {
  PrintDesigner,
} from "../../packages/designer-react/src"
import {
  testCustomPlugin,
} from "../helpers/testPlugin"

describe("plugin renderer", () => {
  it("should render plugin element with plugin dom renderer", async () => {
    const { getByText, container } = render(
      <div style={{ width: 1000, height: 800 }}>
        <PrintDesigner
          template={createEmptyTemplate()}
          templateKind="core"
          plugins={[testCustomPlugin()]}
        />
      </div>,
    )

    fireEvent.click(getByText("Custom Box"))

    await waitFor(() => {
      expect(
        container.querySelector('[data-element-type="test:custom-box"]'),
      ).toBeTruthy()
    })
  })
})
```

---

## 3.4 可能需要修复的点

如果测试失败，大概率是这几个原因：

```txt
1. DesignerRegistryProvider 没从 PluginManager 读取 elements
2. ElementPalette 还是只读 builtinElementDefinitions
3. DesignerCanvas mountLayout 没传 plugin domRenderers
4. DynamicPropertyPanel 没通过 registry.get(element.type).propertySchema 渲染
```

对应修复如下。

### `DesignerRegistryProvider` 应该从 PluginManager 生成 registry

```tsx
import {
  createContext,
  useContext,
  useMemo,
} from "react"
import type {
  ElementRegistry,
} from "@hiprint-re/core"
import {
  useDesignerPluginManager,
} from "../plugin/DesignerPluginProvider"

const DesignerRegistryContext =
  createContext<ElementRegistry | null>(null)

export function DesignerRegistryProvider(
  props: {
    children: React.ReactNode
  },
) {
  const pluginManager = useDesignerPluginManager()

  const registry = useMemo(() => {
    return pluginManager.createElementRegistry()
  }, [pluginManager])

  return (
    <DesignerRegistryContext.Provider value={registry}>
      {props.children}
    </DesignerRegistryContext.Provider>
  )
}

export function useDesignerRegistry(): ElementRegistry {
  const registry = useContext(DesignerRegistryContext)

  if (!registry) {
    throw new Error(
      "[hiprint-re/designer-react] DesignerRegistryProvider is missing.",
    )
  }

  return registry
}
```

### `DesignerCanvas` 应传入插件 DOM renderers

```tsx
const pluginManager = useDesignerPluginManager()

mountRef.current = mountLayout(layout, container, {
  ...ctx.domOptions,
  pageGap: 24,
  renderers: [
    ...(ctx.domOptions?.renderers ?? []),
    ...pluginManager.getDomRenderers(),
  ],
})
```

---

# PR 4：全局测试验证脚本

## 目标

当前问题：

```txt
57 个测试文件，但未验证是否全部通过
```

需要补统一验证命令，不要依赖人工一个个跑。

---

## 4.1 根 `package.json` 增加 scripts

```json
{
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "vitest run",
    "test:all": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "check:tests": "node tools/check-tests.mjs",
    "check:all": "pnpm typecheck && pnpm test:all && pnpm check:docs"
  }
}
```

如果项目是 monorepo，建议保留：

```json
{
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "check": "pnpm typecheck && pnpm test:all"
  }
}
```

---

## 4.2 新增测试文件统计脚本

### `tools/check-tests.mjs`

```js
import fs from "node:fs"
import path from "node:path"
import process from "node:process"
import { spawnSync } from "node:child_process"

const root = process.cwd()

const testFilePatterns = [
  ".test.ts",
  ".test.tsx",
  ".spec.ts",
  ".spec.tsx",
]

const ignoredDirs = new Set([
  "node_modules",
  "dist",
  ".git",
  "coverage",
  ".turbo",
])

function walk(dir, result = []) {
  const entries = fs.readdirSync(dir, {
    withFileTypes: true,
  })

  for (const entry of entries) {
    if (ignoredDirs.has(entry.name)) continue

    const fullPath = path.join(dir, entry.name)

    if (entry.isDirectory()) {
      walk(fullPath, result)
      continue
    }

    if (
      entry.isFile() &&
      testFilePatterns.some((pattern) => entry.name.endsWith(pattern))
    ) {
      result.push(fullPath)
    }
  }

  return result
}

const testFiles = walk(root)
const relativeFiles = testFiles.map((file) => path.relative(root, file))

console.log(`[check-tests] Found ${relativeFiles.length} test files.`)

for (const file of relativeFiles) {
  console.log(`- ${file}`)
}

if (relativeFiles.length === 0) {
  console.error("[check-tests] No test files found.")
  process.exit(1)
}

const result = spawnSync(
  "pnpm",
  ["vitest", "run", "--passWithNoTests"],
  {
    stdio: "inherit",
    shell: process.platform === "win32",
  },
)

if (result.status !== 0) {
  console.error("[check-tests] Vitest failed.")
  process.exit(result.status ?? 1)
}

console.log("[check-tests] All tests passed.")
```

---

## 4.3 GitHub Actions 可选补齐

### `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches:
      - main
      - "refactor/**"
      - "feat/**"
      - "fix/**"
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Tests
        run: pnpm check:tests

      - name: Docs
        run: pnpm check:docs
```

---

# PR 5：Docs 完整性验证

## 目标

当前问题：

```txt
32 个 docs 文件，未全部验证内容完整性
```

Docs 不一定要逐字检查，但至少要有自动检查：

```txt
1. docs 文件数量
2. 是否为空
3. 是否有一级标题
4. 是否包含 TODO / TBD / 待补充
5. phase 文档是否有 Goal / Deliverables / Acceptance
```

---

## 5.1 新增 docs 检查脚本

### `tools/check-docs.mjs`

```js
import fs from "node:fs"
import path from "node:path"
import process from "node:process"

const root = process.cwd()
const docsDir = path.join(root, "docs")

const requiredSectionsByPattern = [
  {
    pattern: /^phase-\d+\.md$/,
    sections: [
      "# ",
      "Goal",
      "Deliverables",
      "Acceptance",
    ],
  },
]

const forbiddenMarkers = [
  "TODO",
  "TBD",
  "待补充",
  "未完成",
]

function fail(message) {
  console.error(`[check-docs] ${message}`)
  process.exitCode = 1
}

function walkMarkdown(dir, result = []) {
  if (!fs.existsSync(dir)) return result

  const entries = fs.readdirSync(dir, {
    withFileTypes: true,
  })

  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name)

    if (entry.isDirectory()) {
      walkMarkdown(fullPath, result)
      continue
    }

    if (entry.isFile() && entry.name.endsWith(".md")) {
      result.push(fullPath)
    }
  }

  return result
}

const files = walkMarkdown(docsDir)
const relativeFiles = files.map((file) => path.relative(root, file))

console.log(`[check-docs] Found ${relativeFiles.length} docs files.`)

if (files.length === 0) {
  fail("No markdown docs found.")
}

for (const file of files) {
  const relative = path.relative(root, file)
  const content = fs.readFileSync(file, "utf8")
  const basename = path.basename(file)

  console.log(`- ${relative}`)

  if (content.trim().length === 0) {
    fail(`${relative} is empty.`)
    continue
  }

  if (!/^#\s+/m.test(content)) {
    fail(`${relative} is missing top-level heading.`)
  }

  for (const marker of forbiddenMarkers) {
    if (content.includes(marker)) {
      fail(`${relative} contains forbidden marker: ${marker}`)
    }
  }

  for (const rule of requiredSectionsByPattern) {
    if (!rule.pattern.test(basename)) continue

    for (const section of rule.sections) {
      if (!content.includes(section)) {
        fail(`${relative} is missing required section: ${section}`)
      }
    }
  }
}

if (process.exitCode) {
  console.error("[check-docs] Docs check failed.")
  process.exit(process.exitCode)
}

console.log("[check-docs] Docs check passed.")
```

---

## 5.2 根 `package.json` 增加

```json
{
  "scripts": {
    "check:docs": "node tools/check-docs.mjs"
  }
}
```

---

## 5.3 可选：生成 docs 状态报告

### `tools/docs-report.mjs`

```js
import fs from "node:fs"
import path from "node:path"

const root = process.cwd()
const docsDir = path.join(root, "docs")
const outputPath = path.join(root, "docs", "_status.md")

function walkMarkdown(dir, result = []) {
  if (!fs.existsSync(dir)) return result

  const entries = fs.readdirSync(dir, {
    withFileTypes: true,
  })

  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name)

    if (entry.isDirectory()) {
      walkMarkdown(fullPath, result)
      continue
    }

    if (
      entry.isFile() &&
      entry.name.endsWith(".md") &&
      entry.name !== "_status.md"
    ) {
      result.push(fullPath)
    }
  }

  return result
}

const files = walkMarkdown(docsDir)

const rows = files.map((file) => {
  const relative = path.relative(root, file)
  const content = fs.readFileSync(file, "utf8")
  const hasTitle = /^#\s+/m.test(content)
  const hasTodo = /TODO|TBD|待补充|未完成/.test(content)

  return {
    file: relative,
    size: content.length,
    hasTitle,
    hasTodo,
  }
})

const markdown = [
  "# Docs Status",
  "",
  `Total docs: ${rows.length}`,
  "",
  "| File | Size | Title | TODO Marker |",
  "|---|---:|---|---|",
  ...rows.map((row) =>
    `| ${row.file} | ${row.size} | ${row.hasTitle ? "yes" : "no"} | ${row.hasTodo ? "yes" : "no"} |`,
  ),
  "",
].join("\n")

fs.writeFileSync(outputPath, markdown)

console.log(`[docs-report] Generated ${path.relative(root, outputPath)}`)
```

package script：

```json
{
  "scripts": {
    "docs:report": "node tools/docs-report.mjs"
  }
}
```

---

# 最终建议的 Issue / PR 拆分

## Issue 1：补齐 Vue Designer Playground

```txt
Title:
fix(playground-vue): integrate PrintDesigner

Tasks:
- [ ] apps/playground-vue 增加 @hiprint-re/designer-vue 依赖
- [ ] App.vue 增加 Designer / Preview 模式切换
- [ ] 接入 PrintDesigner
- [ ] 接入 plugins-basic
- [ ] 验证 template change 后 preview 同步
```

---

## Issue 2：接入 propertySchema 到 DynamicPropertyPanel

```txt
Title:
fix(designer): render properties from ElementDefinition.propertySchema

Tasks:
- [ ] designer-core 增加 getByPath / setByPathImmutable
- [ ] designer-core 增加 createUpdateElementPropertyCommand
- [ ] designer-react useDesignerCommands 增加 updateElementProperty
- [ ] DynamicPropertyPanel 从 registry.get(element.type).propertySchema 渲染
- [ ] PropertyField 支持 text/number/boolean/select/color/textarea/json
```

---

## Issue 3：验证插件元素属性面板

```txt
Title:
test(plugin): verify plugin element property schema flow

Tasks:
- [ ] 新增 testCustomPlugin
- [ ] 测试插件元素出现在 ElementPalette
- [ ] 测试插件元素 propertySchema 出现在 DynamicPropertyPanel
- [ ] 测试修改插件字段后 onChange 返回正确 template
- [ ] 测试插件 DOM renderer 生效
```

---

## Issue 4：全局测试验证

```txt
Title:
test(repo): add global test verification script

Tasks:
- [ ] 新增 tools/check-tests.mjs
- [ ] package.json 增加 check:tests
- [ ] package.json 增加 check:all
- [ ] CI 中执行 pnpm check:tests
```

---

## Issue 5：Docs 完整性验证

```txt
Title:
docs(repo): add docs completeness checker

Tasks:
- [ ] 新增 tools/check-docs.mjs
- [ ] 新增 tools/docs-report.mjs
- [ ] package.json 增加 check:docs
- [ ] package.json 增加 docs:report
- [ ] CI 中执行 pnpm check:docs
```

---

# 建议执行顺序

```txt
1. PR 2：先修 propertySchema 接线
2. PR 3：补插件属性测试，验证 Phase 10 是否真通
3. PR 1：补 Vue Designer Playground
4. PR 4：补测试总入口
5. PR 5：补 docs 检查
```

原因是：`DynamicPropertyPanel + propertySchema` 是 Phase 8 / Phase 10 的共同基础，先修它，Vue Playground 和插件验证才有意义。

---

# 最终验收命令

完成后建议统一用：

```bash
pnpm install
pnpm typecheck
pnpm check:tests
pnpm check:docs
pnpm --filter @hiprint-re/playground-vue dev
```

最终达标状态应变成：

```txt
Phase 8:
  ✅ Vue Designer Playground 已接入 PrintDesigner

Phase 5 / Phase 8:
  ✅ ElementDefinition.propertySchema 已驱动 DynamicPropertyPanel

Phase 10:
  ✅ 插件元素 propertySchema 可渲染、可编辑、可写回 template

全局测试:
  ✅ 57 个测试文件可被 check:tests 统一验证

全局文档:
  ✅ 32 个 docs 文件可被 check:docs 统一验证
```
